/**
 * Projects Feature
 *
 * Public API for projects feature module.
 * Features CANNOT import from other features - only from shared/, lib/, utils/, config/.
 */

export { ProjectSelector } from './components/project-selector';
